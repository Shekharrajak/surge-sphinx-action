name: Surge Sphinx deployment
on: [push, pull_request]

jobs:
  build:
    name: Surge Sphinx action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: './surge-sphinx-action'
      env:
        BUILD_DIR: $(echo $PWD'/_build')
